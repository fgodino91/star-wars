
doctype html
html
  head
    block title
      title STAR WARS -- Uptake
    block head
      meta(name='viewport' content="width=device-width, initial-scale=1, shrink-to-fit=no")
      meta(name='robots' content='noindex')
      link(rel="stylesheet" href=bundle('global.css'))
      link(rel='shortcut icon', href='/favicon.ico')
      script(type="text/javascript" src=bundle('global'))

  body
    block body
    block pageScripts
